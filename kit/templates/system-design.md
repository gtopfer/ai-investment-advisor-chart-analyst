# System Design template

O arquivo humano fica na **raiz do repositório**: `../../system-design.md`  
(junto com `agents.md` e `CHANGELOG.md`).

Este path em `kit/templates/` é só referência; **não** peça ao usuário editar dentro de `kit/`.

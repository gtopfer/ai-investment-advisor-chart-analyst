# Specs de feature (histórico)

Pasta de especificações de feature e correções do produto.

Migradas do antigo DevKit (`.ai/specs/`) em 2026-08-07. O processo atual é **jojo-ai** — ver `agents.md` e `system-design.md` na raiz.

## Organização

1. **Nomeação:** prefixo numérico — ex.: `001_filtro_status.md`
2. **Novas features:** preferir template `kit/templates/spec.md` e rastrear em `CHANGELOG.md` (raiz)
3. **Detalhe técnico legado:** `docs/technical-spec.md`

## Já no repositório

SPEC-000…035 (setup, features base, higiene, backlog de maturidade). Índice em [`docs/README.md`](../README.md) e [`docs/BACKLOG.md`](../BACKLOG.md).
